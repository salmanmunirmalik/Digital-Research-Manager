# Digital Research Manager – Render Environment Template
# -----------------------------------------------------
# Copy this file's contents into Render's "Environment" section as individual
# key/value pairs (one per line). Replace placeholder values before deploying.

# --- Core Application ---
NODE_ENV=production
PORT=5002                         # Render will override if needed; keep for local parity
FRONTEND_URL=https://digital-research-manager-t267.onrender.com

# PostgreSQL connection (Render Postgres)
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DATABASE        # <- replace with real URL
DB_SSL=true                   # Keep SSL enabled for managed Postgres providers
DB_POOL_MAX=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# Authentication & security
JWT_SECRET=replace-with-strong-random-secret                    # e.g. `openssl rand -hex 32`
ENABLE_DEMO_AUTH=false                                          # set true only if you need demo mode
DEMO_AUTH_TOKEN=demo-token-123                                  # ignored when ENABLE_DEMO_AUTH=false
ENCRYPTION_KEY=replace-with-64-hex-characters                   # `openssl rand -hex 32`

# Optional external integrations
OPENAI_API_KEY=
TOGETHER_API_KEY=
MATERIALS_PROJECT_API_KEY=
LENS_API_TOKEN=
PYTHON_STATS_SERVICE_URL=
REDIS_URL=

# --- Frontend (Vite) ---
VITE_API_URL=https://digital-research-manager-t267.onrender.com/api
VITE_GEMINI_API_KEY=

# Testing / automation (optional)
BASE_URL=https://digital-research-manager-t267.onrender.com
BACKEND_URL=https://digital-research-manager-t267.onrender.com
ENVIRONMENT=production

